{% trans_default_domain 'FOSUserBundle' %}

<div class="card">
    <div class="card-header">
        <strong>View User</strong>
        {#            <small>List</small>#}
    </div>
    <div class="card-body">     
        <p class="label label-primary"><b>{{ 'profile.show.username'|trans }}:</b> {{ user.username }}</p>
        <span class="label-custom"><b>{{ 'profile.show.email'|trans }}:</b> {{ user.email }}</span>
    </div>
</div>

    
    
    